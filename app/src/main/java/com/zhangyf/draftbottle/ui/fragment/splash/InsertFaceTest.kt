package com.zhangyf.draftbottle.ui.fragment.splash



fun test(){
/*    //测试
    binding.fbAdd.setOnClickListener {

        val inputStream: InputStream = resources.openRawResource(R.drawable.c10)

        val mBitmap = BitmapFactory.decodeStream(inputStream)

        //人脸
        val currentFeature = TPFaceApi.instance.extractFeature(mBitmap)?.faces(0)

        //录入信息至系统 测试用 真实人脸特征信息从服务器拉取，本地不新增人脸
        SharedPrefModel.setUserModel {
            studentsList = studentsList.apply {
                add(
                    0,
                    Student(
                        System.currentTimeMillis(),
                        currentFeature?.convertToArray() ?: floatArrayOf()
                    )
                )
            }
            showToast("录入人脸信息成功！")
        }
    }*/
}